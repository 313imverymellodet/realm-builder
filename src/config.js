// Shared world constants (imported by world.js, game.js, rival.js).
export const GRID_N = 24;   // grid is GRID_N x GRID_N cells
export const CELL = 4;      // world units per cell

// The rival AI's settlement sits to the north, across a river.
export const RIVAL_CENTER = { x: 0, z: -150 };
